name: Demo CI ENV Secrets Inputs

on:
    workflow_dispatch:
        

jobs:
    build:
        runs-on: ubuntu-latest

        steps:
            - name: Say Hello
              run: echo "Hello from the Demo CI ENV Secrets Inputs workflow!"

            - name: Install
              env:
                APP_ENV: dev
                DB_PASSWORD: ${{ secrets.DB_PASSWORD }}
              run: echo "Environment $APP_ENV using secret DB_PASSWORD $DB_PASSWORD"